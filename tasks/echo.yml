---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: resource-src
    # path: resource-src/job-inputs/

params:
  test_env: ((test_env))
run:
  path: bash
  args: [which bash]
  # path: resource-src/scripts/bash/echo.sh
