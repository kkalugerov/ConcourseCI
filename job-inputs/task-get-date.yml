---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
  - name: resource-src
    # path: resource-src/job-inputs/

run:
  path: resource-src/job-inputs/get-date.sh
