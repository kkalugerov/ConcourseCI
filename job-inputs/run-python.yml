---
platform: linux

image_resource:
  type: docker-image
  source: {repository: python, tag: alpine}

inputs:
  - name: resource-src
    path: resource-src/job-inputs/

run:
  # path: sh
  # args:
  # - -exc
  # - |
  #   python3 resource-src/job-inputs/script.py
  path: resource-src/execute.sh
