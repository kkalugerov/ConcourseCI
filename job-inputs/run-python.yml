---
platform: linux

image_resource:
  type: docker-image
  source: {repository: python, tag: alpine}

inputs:
  - name: resource-src
  - name: resource-app
    # path: src/github.com/kkalugerov/ConcourseCI

run:
  path: resource-src/job-inputs/execute.sh
