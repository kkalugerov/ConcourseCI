---
platform: linux

image_resource:
  type: docker-image
  source: {repository: alpine}

inputs:
  - name: resource-src
    # path: resource-src/job-inputs/

run:
  path: resource-src/job-inputs/echo.sh
